# Pinned for Hawaii's NVIDIA driver 535 / CUDA 12.2 runtime. torch cu121 and
# onnxruntime-gpu 1.17.x both run against that driver; do not bump blindly
# (a CUDA 12.4+ build aborts on kernel launch, the Ornith failure mode).
--extra-index-url https://download.pytorch.org/whl/cu121
torch==2.2.2+cu121
torchvision==0.17.2+cu121
open_clip_torch==2.24.0
onnxruntime-gpu==1.17.1
insightface==0.7.3
opencv-python-headless==4.10.0.84
Pillow==10.3.0
numpy==1.26.4
fastapi==0.111.0
uvicorn[standard]==0.30.1
pydantic==2.7.4
