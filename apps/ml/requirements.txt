# Pinned for Hawaii's NVIDIA driver 535 / CUDA 12.2 runtime. onnxruntime-gpu
# 1.18.x is the CUDA-12.2 + cuDNN-8.9 build (1.17 is CUDA 11 and can't find
# libcublasLt.so.12; 1.19+ needs cuDNN 9). torch cu121 also runs on driver 535.
# Do not bump blindly — a CUDA 12.4+ build aborts on kernel launch (Ornith).
--extra-index-url https://download.pytorch.org/whl/cu121
torch==2.2.2+cu121
torchvision==0.17.2+cu121
open_clip_torch==2.24.0
onnxruntime-gpu==1.18.1
insightface==0.7.3
opencv-python-headless==4.10.0.84
Pillow==10.3.0
numpy==1.26.4
fastapi==0.111.0
uvicorn[standard]==0.30.1
pydantic==2.7.4
